# GoDad 后端服务环境配置示例
# 将此文件复制为 .env 并根据实际情况修改配置

# 服务器配置
SERVER_ENV=development          # 运行环境: development, production, testing
SERVER_HOST=127.0.0.1          # 服务器主机
SERVER_PORT=8888               # 服务器端口
FRONTEND_URL=http://127.0.0.1:3333  # 前端地址

# 数据库配置
DB_HOST=127.0.0.1              # 数据库主机
DB_PORT=3306                   # 数据库端口
DB_USER=root                   # 数据库用户名
DB_PASSWORD=                   # 数据库密码
DB_NAME=godad                  # 数据库名称
DB_AUTO_MIGRATE=true           # 是否自动迁移（开发环境建议true，生产环境建议false）

# JWT配置
JWT_SECRET=your-jwt-secret-key-change-in-production  # JWT密钥（生产环境必须修改）
JWT_EXPIRE_HOURS=24            # JWT过期时间（小时）

# 阿里云OSS配置（文件上传）
OSS_ENDPOINT=oss-cn-beijing.aliyuncs.com  # OSS端点
OSS_ACCESS_KEY_ID=             # OSS访问密钥ID
OSS_ACCESS_KEY_SECRET=         # OSS访问密钥密码
OSS_BUCKET_NAME=godad          # OSS存储桶名称
OSS_CUSTOM_DOMAIN=             # OSS自定义域名（可选，用于生成访问URL）

# 邮件服务配置（SMTP）
# 如果不配置以下参数，将使用控制台模拟发送
SMTP_HOST=smtp.gmail.com        # SMTP服务器地址
SMTP_PORT=587                   # SMTP端口
SMTP_USERNAME=                  # 邮箱账号
SMTP_PASSWORD=                  # 邮箱密码或应用专用密码
SMTP_FROM=                      # 发件人邮箱
SMTP_FROM_NAME=GoDad育儿平台     # 发件人名称

# 常用邮件服务商配置参考：
# Gmail (推荐) - 需要开启两步验证并使用应用专用密码
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# QQ邮箱 - 需要开启SMTP服务并获取授权码
# SMTP_HOST=smtp.qq.com
# SMTP_PORT=587

# 163邮箱 - 需要开启SMTP服务并获取授权码
# SMTP_HOST=smtp.163.com
# SMTP_PORT=465                   # 使用SSL模式

# Outlook/Hotmail
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# 生产环境配置建议
# SERVER_ENV=production
# DB_AUTO_MIGRATE=false         # 生产环境关闭自动迁移
# 使用独立的迁移脚本: go run scripts/migrate.go --action=up --force